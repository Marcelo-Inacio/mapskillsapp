(function() {
	'use strict';

	angular
		.module('mapskill')
		.factory('AdminService', ['$http', '$q', AdminService]);

		/** @ngInject */
		function AdminService($http, $q) {
			return {
				
			};
						
		}
})();
