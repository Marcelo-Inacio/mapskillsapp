(function() {
	'use strict';

	angular
		.module('mentor')
		.factory('MentorService', ['$http', '$q', MentorService]);

		/** @ngInject */
		function MentorService($http, $q) {
			return {
				
			};
						
		}
})();
